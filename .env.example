# EdTech Platform - Environment Variables Template
# Copy this file to .env.local for local development

# ============================================
# Application
# ============================================
NODE_ENV=development
SERVICE_NAME=identity-service

# ============================================
# Database (Local PostgreSQL via Docker)
# ============================================
DATABASE_URL=postgresql://edtech:edtech_dev_password@localhost:5432/edtech_identity

# ============================================
# Redis (Local Redis via Docker)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=edtech_redis_password

# ============================================
# AWS Credentials (for local development)
# ============================================
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1

# ============================================
# AWS Cognito (Real AWS, created by Terraform local environment)
# These values will be output by terraform apply in terraform/environments/local/
# ============================================
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
COGNITO_REGION=us-east-1

# ============================================
# Observability (Local Development)
# ============================================
# Disable CloudWatch to avoid costs during local development
METRICS_ENABLED=false
LOGS_CLOUDWATCH=false

# ============================================
# Server Ports
# ============================================
# Public API port (client-facing)
PORT=3000
# Internal API port (service-to-service)
INTERNAL_PORT=3001

# ============================================
# NOTES:
# ============================================
# 1. For local development:
#    - Use local PostgreSQL (docker-compose up postgres)
#    - Use real AWS Cognito (created via Terraform)
#    - Disable CloudWatch (METRICS_ENABLED=false)
#
# 2. Get Cognito credentials:
#    cd terraform/environments/local
#    terraform apply
#    terraform output
#
# 3. Never commit .env.local to git!
